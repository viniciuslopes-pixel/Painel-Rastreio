# Exemplo para Streamlit Community Cloud (Settings → Secrets).
# NÃO commite este arquivo com valores reais; use só como referência.
#
# No painel da Cloud, cole um TOML assim (ajuste os valores):

databricks_host = "dbc-xxxx.cloud.databricks.com"
databricks_http_path = "/sql/1.0/warehouses/xxxxxxxx"
databricks_token = "dapixxxxxxxx"

# Opção A — JSON do painel em uma linha (minifique com uma ferramenta JSON ou editor):
# nuvem_config_json = '{"catalog_schema":"...","tabs":{...}}'

# Opção B — JSON multilinha (TOML):
# nuvem_config_json = """
# {"catalog_schema": "seu_catalogo.schema", "tabs": { ... }}
# """

# Se você commitar nuvem_envio_rastreio_config.json no GitHub (repo privado), pode omitir nuvem_config_json.
